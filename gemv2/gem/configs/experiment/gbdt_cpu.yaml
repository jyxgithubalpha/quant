defaults:
  - base
  - _self_

n_trials: 1
resource_request:
  trial_cpus: 8
  final_train_cpus: 8
visualization:
  enabled: true
  metrics:
    metric_names:
      - daily_ic
      - daily_icir_expanding
      - daily_top_ret
      - daily_top_ret_std
      - daily_top_ret_relative_improve_pct
      - daily_model_benchmark_corr
